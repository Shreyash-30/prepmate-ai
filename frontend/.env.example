# ============================================================================
# FRONTEND LOCAL ENVIRONMENT OVERRIDES
# ============================================================================
# This file is OPTIONAL and FOR LOCAL DEVELOPMENT ONLY
# Copy this to .env.local and add any frontend-specific overrides
#
# The system will first load from the CENTRAL .env file at project root
# (Vite will automatically use VITE_* prefixed variables),
# then apply any local overrides from .env.local
#
# See the central .env.example at ../.env.example for all available options
# ============================================================================

# Frontend-Specific Overrides (optional)
# VITE_API_BASE_URL=http://localhost:5000/api
# VITE_AI_SERVICE_URL=http://localhost:8000/api
# VITE_APP_NAME=PrepMate AI
# VITE_APP_VERSION=1.0.0
# VITE_DEBUG_MODE=false

